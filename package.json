{
  "name": "paperflow-publisher",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Search papers, extract figures, write WeChat-ready Markdown, and export portrait PDFs.",
  "scripts": {
    "generate": "node src/cli.js",
    "doctor": "node src/doctor.js",
    "test": "node --test",
    "check": "node --check src/cli.js && node --check src/pipeline.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.901.0",
    "@napi-rs/canvas": "^0.1.80",
    "dotenv": "^17.2.2",
    "fast-xml-parser": "^5.2.5",
    "marked": "^16.3.0",
    "pdfjs-dist": "^5.4.149",
    "playwright-core": "^1.55.0",
    "sharp": "^0.34.4"
  }
}
